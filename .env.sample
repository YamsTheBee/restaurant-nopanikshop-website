# ===============================
# 🌿 ENV SAMPLE – FRONTEND (Vite)
# ===============================

# URL du backend API
VITE_API_URL=

# Clé publique Stripe (si tu ajoutes les paiements)
VITE_STRIPE_PUBLIC_KEY=

# Base URL du frontend (local ou déploiement)
VITE_BASE_URL=

# Nom de l’application
VITE_APP_NAME=

# ID analytics (Google Analytics, Matomo…)
VITE_ANALYTICS_ID=

# CDN pour les images (optionnel)
VITE_ASSET_CDN=


# Comment utiliser ces variables dans le frontend ?
# Exemple dans un fichier React/TSX :
# const apiUrl = import.meta.env.VITE_API_URL;
# console.log("API backend :", apiUrl);
# Exemple pour un fetch :
# fetch(`${import.meta.env.VITE_API_URL}/actions`)
#   .then(res => res.json())
#   .then(data => console.log(data));
# ===============================